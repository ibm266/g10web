# Moved → `fable/`

All design context for Claude Design lives in **`../fable/`**.

Point Claude Design at the `fable/` folder. See `fable/CLAUDE.md` for load tiers.

This folder is kept as a redirect only.
